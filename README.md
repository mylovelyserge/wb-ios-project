# 🍔 Eat & Pay — учебное приложение доставки еды

iOS-приложение доставки еды, которое я пишу с нуля на SwiftUI, осваивая iOS-разработку. Проект строится последовательно, этап за этапом: от каталога товаров до корзины, избранного и поиска по всему ассортименту.

![Swift](https://img.shields.io/badge/Swift-6-orange?logo=swift)
![SwiftUI](https://img.shields.io/badge/SwiftUI-blue?logo=swift)
![Platform](https://img.shields.io/badge/Platform-iOS-lightgrey?logo=apple)
![Status](https://img.shields.io/badge/Status-в%20разработке-yellow)

---

## ✨ Что уже умеет приложение

- **Каталог категорий** — сетка категорий с картинками, данные с сервера
- **Список товаров** — товары выбранной категории: фото, цена, вес, рейтинг
- **Экран деталей товара** — открывается как модальный sheet:
  - фото на всю ширину, цена, название, описание
  - рейтинг со звёздами и счётчиком отзывов (с правильным склонением: 1 отзыв / 2 отзыва / 5 отзывов)
  - кнопка «В корзину» с фирменным градиентом и подтверждением по нажатию
- **Вкладки** — TabView «Каталог» / «Избранное» / «Корзина», каждая со своей независимой навигацией
- **Корзина**:
  - добавление товара из карточки каталога и с экрана деталей
  - один товар = одна позиция, повторное добавление увеличивает количество
  - кастомный контрол «− N +», удаление позиции при нуле
  - подсчёт итоговой суммы и количества товаров
  - пустое состояние и живой бейдж со счётчиком на вкладке
- **Избранное**:
  - сердечко прямо на карточке товара и на экране деталей
  - отдельная вкладка со списком избранного
  - хранится локально в `Set<String>` — проверка вхождения за O(1)
- **Поиск** — отдельный полноэкранный экран, доступный из каталога и избранного:
  - ищет по всему ассортименту, а не по текущей категории
  - подсказки по мере ввода с задержкой (debounce), результаты не пересчитываются на каждую букву
  - история недавних запросов, тап по строке повторяет поиск
  - пустое состояние `ContentUnavailableView.search`, когда ничего не найдено
  - из результатов можно открыть товар, добавить в корзину и в избранное
- **Дизайн-система** — цвета и типографика вынесены в отдельный Swift Package

## 🛠 Стек и технологии

| Область | Решение |
|---|---|
| UI | SwiftUI |
| Язык | Swift 6 |
| Архитектура | MV + Services (`@Observable`-сервисы без ViewModel) |
| Общее состояние | `@Observable` + Environment (корзина, избранное и поиск — по одному экземпляру на всё приложение) |
| Конкурентность | `async/await`, `withTaskGroup` для параллельных запросов, `Task` + отмена для debounce |
| Дизайн-система | Локальный Swift Package `DesignSystem` |
| Сетевой слой | [swift-openapi-generator](https://github.com/apple/swift-openapi-generator) — клиент генерируется из `openapi.json` на этапе сборки |
| Транспорт | swift-openapi-urlsession |
| Авторизация | Bearer JWT через кастомный `ClientMiddleware` |
| Логирование | Собственный `LoggingMiddleware` (метод / путь / статус) |

## 🏗 Архитектура

Проект организован по слоям:

```
wb-ios-project/
├── Models/               # Доменные модели и моки
├── Services/             # Сервисы (@Observable) и middlewares
├── Views/                # Экраны и переиспользуемые компоненты
├── DesignSystem/         # Swift Package: DSColors, DSTypography
└── openapi.json          # Спека, из которой генерируется сетевой клиент
```

Ключевые принципы:

- **Своя доменная модель + маппинг из DTO** — UI не зависит от формата API
- **Single source of truth** — у каждого факта одно место жительства, производные значения вычисляются, а не хранятся
- **Экраны самодостаточны** — детальный экран принимает `id` и сам загружает данные, поэтому его можно открыть откуда угодно
- **`NavigationStack` внутри каждой вкладки** — вкладки сохраняют своё состояние независимо
- **Глупые компоненты** — переиспользуемые вью принимают данные и замыкания, не зная о сервисах
- **Методы, которые возвращают, и методы, которые сохраняют** — разделены: `fetch` только отдаёт данные и потому безопасен для параллельных вызовов, `load` кладёт результат в свойство сервиса
- **Структура данных под задачу** — избранному нужен только факт членства (`Set`), корзине — количество (`Dictionary`), истории поиска — порядок (`Array`)

## 🚀 Как запустить

1. Клонируй репозиторий:
   ```bash
   git clone https://github.com/mylovelyserge/wb-ios-project.git
   ```
2. Создай файл `Secrets.swift` в корне проекта (он в `.gitignore` и не хранится в репозитории):
   ```swift
   enum Secrets {
       static let apiToken = "твой-JWT-токен"
   }
   ```
3. Открой проект в Xcode и запусти — сетевой клиент сгенерируется автоматически при первой сборке.

> **Требования:** Xcode с iOS 26 SDK, симулятор или устройство на iOS 26+.

## 🗺 Дорожная карта

- [x] Каталог категорий
- [x] Список товаров в категории
- [x] Экран деталей товара (sheet)
- [x] TabView: Каталог / Избранное / Корзина
- [x] Корзина: `CartService`, список товаров, контрол количества «− N +»
- [x] Итоговая сумма и кнопка «Оформить заказ»
- [x] Дизайн-система в отдельном Swift Package (DSColors, DSTypography)
- [x] Избранное: `FavoriteService`, сердечко на карточке, отдельный экран
- [x] Поиск по всему каталогу с подсказками, debounce и историей запросов
- [ ] Тюнинг визуала и анимации

## 📚 Чему я учусь в этом проекте

Проект учебный: каждое решение здесь принято осознанно и разобрано «почему так». Среди пройденного:

- **навигация** — `NavigationStack`, модальные экраны через `.sheet(item:)` и `.fullScreenCover`, закрытие через `@Environment(\.dismiss)`, `TabView` и независимое состояние вкладок
- **состояние** — `@State`, `@Observable`-сервисы, общее состояние приложения через Environment, `@FocusState` для управления клавиатурой
- **конкурентность** — параллельная загрузка категорий через `withTaskGroup`, debounce на `Task` + `Task.sleep` + `Task.isCancelled` (без Combine), отмена устаревшей работы
- **алгоритмы и структуры данных** — выбор структуры под задачу (`Set` / `Dictionary` / `Array`), дедупликация за один проход через `Set` вместо квадратичного поиска по массиву, регистронезависимое сравнение строк
- **вёрстка** — стеки и выравнивание по базовой линии, `safeAreaInset`, `ContentUnavailableView`, тап-зоны и `contentShape`, размеры на `AsyncImage`, а не внутри его веток
- **данные** — генерация сетевого слоя из OpenAPI-спеки, маппинг DTO в доменные модели, работа с опционалами (chaining, nil-coalescing)
- **проектирование** — инварианты модели, вычисляемые свойства вместо хранимых, «глупые» переиспользуемые компоненты, внедрение зависимостей через `init` вместо создания их внутри

---

*Проект в активной разработке — README обновляется по мере прохождения этапов.*
